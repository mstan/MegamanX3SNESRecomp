/* Game-specific RAM variable declarations for Mega Man X3.
 *
 * The runner includes this unconditionally (snes/cpu.c, snes/snes.c), so it
 * must exist even when empty. Populate as WRAM locations are identified;
 * generated code refers to memory by raw address until then.
 *
 * g_ram is declared by snesrecomp/runner/src/common_rtl.h.
 */

#ifndef VARIABLES_H
#define VARIABLES_H

#include "types.h"

#endif /* VARIABLES_H */
